# Инструкция по очистке канала и подготовке к продакшену

## Проблема

В Telegram **НЕЛЬЗЯ** физически переместить сообщения или изменить их порядок. Сообщения всегда в хронологическом порядке.

## Решение

1. **Очистка канала** от тестовых данных
2. **Закрепленный топ** идей по голосам (обновляется автоматически)

---

## Шаг 1: Создать таблицу system_messages

Выполни в Supabase SQL Editor:

```sql
-- Скопируй из файла system-messages.sql
```

---

## Шаг 2: Очистить канал и базу данных

⚠️ **ВНИМАНИЕ**: Это удалит ВСЕ данные!

```bash
node cleanup-production.js
```

Скрипт:
- Удалит все посты из канала @ai_requests
- Очистит таблицы: requests, votes, payments, conversations
- Запросит двойное подтверждение

---

## Шаг 3: Запустить бота с чистого листа

После очистки бот работает как на продакшене:
- Все новые идеи проходят Customer Development
- AI валидирует ценность и готовность платить
- Кнопки показываются только после полного описания

---

## Опционально: Закрепленный топ идей

Создать/обновить топ-10 идей по голосам:

```bash
node update-top-ideas.js
```

Что делает:
- Берет топ-10 идей по vote_count из базы
- Создает красивый пост с медалями 🥇🥈🥉
- Закрепляет в канале
- При следующем запуске - обновляет существующий пост

Запускать можно:
- Вручную когда нужно обновить топ
- Добавить в cron для автообновления раз в час
- Вызывать автоматически после каждого голосования (добавить в код бота)

---

## Автоматическое обновление топа

Чтобы топ обновлялся после каждого голоса, добавь в `index-botpress.js` после обновления vote_count:

```javascript
// После успешного голосования
import { exec } from 'child_process';

// Обновить топ в канале
exec('node update-top-ideas.js', (error) => {
    if (error) console.log('⚠️ Cannot update top:', error.message);
});
```

---

## Текущее состояние

✅ Канал: @ai_requests  
✅ База: Supabase с таблицами requests, votes, payments, conversations  
✅ AI: Customer Development с 7-8 вопросами  
✅ Валидация: Готовность платить, реализуемость, ценность  
✅ Сортировка: Закрепленный топ по голосам  

🎯 Готово к продакшену после очистки тестовых данных!
