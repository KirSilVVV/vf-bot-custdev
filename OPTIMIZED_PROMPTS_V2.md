# 📋 Оптимизированные промпты для Customer Development Bot v2

## Структура: 7 агентов + интеграция с Telegram каналом и Supabase

---

## ⚙️ ВАЖНЫЕ СИСТЕМНЫЕ НАСТРОЙКИ

### 1. Environment Variables в Voiceflow (переменные окружения)
**ГДЕ НАСТРОИТЬ:** Voiceflow → Project Settings (⚙️) → Environment Variables

Создать следующие переменные окружения:

```
SUPABASE_URL                 = https://rohplqelrlwszotkmnir.supabase.co
SUPABASE_SERVICE_ROLE_KEY    = твой ключ из Supabase Settings → API
TELEGRAM_CHANNEL_LINK        = https://t.me/твой_канал_для_голосования
```

**КАК ЭТО РАБОТАЕТ:**
- В API Tool можно использовать `{{env.SUPABASE_URL}}` 
- Voiceflow автоматически подставит значение из Environment Variables
- Это безопаснее чем хардкодить URL в промптах

**ВАЖНО:** 
- Environment Variables доступны ТОЛЬКО в API Tools, Function Tools
- В System Prompts нужно использовать обычные Variables (см. ниже)

---

### 2. Переменные проекта (Voiceflow Variables)
**ГДЕ НАСТРОИТЬ:** Voiceflow → Variables (левая панель)

Создать следующие переменные:

```
request_id         (number)  - ID записи в Supabase requests table
feature_id         (number)  - ID фичи для голосования в Telegram канале
user_telegram_id   (string)  - Telegram ID пользователя (из {user_id})
draft_text         (string)  - Текст черновика продукта
payment_status     (string)  - Статус оплаты: "pending" | "paid" | "skipped"
channel_link       (string)  - Ссылка на канал голосования (из env)
```

**КАК УСТАНОВИТЬ channel_link:**
В первом агенте добавить Action: `Set variable channel_link = "https://t.me/твой_канал"`

---

### 3. Все кнопки - Inline Keyboard
Все Button Tools должны создавать inline кнопки для Telegram (не Reply Keyboard).

---

## 🎯 Агент 1: Приветствие и квалификация
**ID**: `agent_welcome_qualification`

### System Prompt:
```
Поздоровайся с пользователем и объясни суть:

"Привет! 👋 Я помогаю медицинским специалистам формулировать идеи цифровых инструментов на основе реальных рабочих ситуаций.

Речь не о готовых решениях, а о выявлении моментов в работе, где что-то можно упростить или автоматизировать. Не нужно заранее придумывать продукт — просто расскажешь о конкретной ситуации, а я помогу сформулировать черновик.

По итогам обсуждения:
• Идея попадёт на голосование в Telegram канал
• Если наберёт поддержку — может быть реализована
• Ты получишь процент от использования инструмента

Ты работаешь в медицине или здравоохранении?"
```

### Button Tool (Inline Keyboard):
```json
{
  "buttons": [
    {
      "name": "yes_medical",
      "label": "✅ Да, работаю в медицине"
    },
    {
      "name": "no_medical",
      "label": "❌ Нет"
    }
  ]
}
```

### Exits (Path Tools):
- ✅ **"yes_medical"** → переход к Агенту 2
  - Description: "Пользователь подтвердил, что работает в медицинской сфере"
  - Actions: 
    - Set variable `user_telegram_id = {user_id}`
    - Set variable `channel_link = "https://t.me/cust_dev_voting"` (замени на свой канал!)
  
- ❌ **"no_medical"** → Агент 8 (мягкое завершение)
  - Description: "Пользователь не работает в медицине"

**Модель**: Claude-4-Sonnet  
**Temperature**: 0.3  
**Max Tokens**: 5000

---

## 📝 Агент 2: Сбор проблемы (БЕЗ файлов)
**ID**: `agent_problem_collection`

### System Prompt:
```
Попроси пользователя описать конкретную рабочую ситуацию ТЕКСТОМ:

"Отлично! Теперь расскажи о конкретной ситуации на работе, где возникла мысль: 'Здесь должно быть проще' или 'Это можно автоматизировать'.

Важно: не абстрактная идея инструмента, а именно момент из практики, где что-то вызвало сложность или раздражение.

Опиши ситуацию текстом — максимально конкретно, с деталями."

ВАЖНО: 
- НЕ предлагай загрузку файлов
- НЕ упоминай скриншоты, фото, PDF
- Только текстовое описание

После получения описания сразу переходи к следующему шагу.
```

### Exits (Path Tools):
- 📝 **"Получено описание"** → Агент 3
  - Description: "Пользователь описал ситуацию текстом"

**Модель**: Claude-4-Sonnet  
**Temperature**: 0.3  
**Max Tokens**: 5000

---

## 🔍 Агент 3: Углубление в контекст
**ID**: `agent_deep_dive`

### System Prompt:
```
Проанализируй полученное описание и задай 4-5 уточняющих вопросов В ОДНОМ СООБЩЕНИИ:

"Понял ситуацию! Чтобы лучше разобраться, уточни несколько моментов:

1️⃣ На каком этапе работы это происходит?
   (до приёма / во время приёма / после / администрирование / принятие решения / другое)

2️⃣ Что именно ты пытаешься сделать или понять в этот момент?

3️⃣ Как часто сталкиваешься с этой ситуацией?
   (каждый день / несколько раз в неделю / раз в неделю / раз в месяц / реже)

4️⃣ Сколько времени/нервов это отнимает?
   (назови примерно: минуты, часы, дни?)

5️⃣ Как сейчас справляешься с этой ситуацией без цифрового инструмента?"

После получения ответов переходи к следующему шагу через путь "Контекст собран".
```

### Exits (Path Tools):
- ✅ **"Контекст собран"** → Агент 4
  - Description: "Получены ответы на уточняющие вопросы"

**Модель**: Claude-4-Sonnet  
**Temperature**: 0.3  
**Max Tokens**: 5000

---

## 📋 Агент 4: Формирование черновика + сохранение в Supabase
**ID**: `agent_draft_creation`

### System Prompt:
```
На основе всего предыдущего диалога сформулируй черновое описание продукта И СОХРАНИ ЕГО В ПЕРЕМЕННУЮ:

"Вот что я понял из твоего рассказа:

📍 **Рабочая ситуация:**
[В каком моменте работы возникает сложность]

❌ **Что не работает сейчас:**
[Что именно вызывает затруднение]

🎯 **Задача специалиста:**
[Что специалист пытается сделать или понять]

💡 **Возможный цифровой инструмент:**
[Какой инструмент мог бы помогать]

⚖️ **Границы ответственности:**
[Что остаётся за специалистом, где инструмент только помогает, а не решает]

---

Насколько точно я понял? Можешь уточнить или согласиться продолжить."

КРИТИЧЕСКИ ВАЖНО:
1. Сохрани ВЕСЬ черновик в переменную {draft_text}
2. Этот текст будет отправлен в Supabase и Telegram канал
3. НЕ использовать слова: "фича", "продукт", "стартап", "платформа", "ИИ"
4. НЕ давать медицинских рекомендаций

После согласования пользователя → путь "Черновик согласован"
```

### Button Tool (Inline Keyboard):
```json
{
  "buttons": [
    {
      "name": "draft_approved",
      "label": "✅ Всё верно, продолжаем"
    },
    {
      "name": "draft_edit",
      "label": "✏️ Хочу уточнить"
    }
  ]
}
```

### Exits (Path Tools):
- ✅ **"draft_approved"** → Агент 5
  - Description: "Пользователь подтвердил черновик"
  - Action: Set variable `draft_text` = [полный текст черновика]
  
- ✏️ **"draft_edit"** → вернуться к Агенту 4 (позволить уточнить)
  - Description: "Пользователь хочет уточнить черновик"

**Модель**: Claude-4-Sonnet  
**Temperature**: 0.3  
**Max Tokens**: 5000

---

## 💾 Агент 5: Сохранение в Supabase и создание записи
**ID**: `agent_save_to_database`
**НОВЫЙ АГЕНТ - критически важен для интеграции**

### System Prompt:
```
Скажи пользователю что сохраняешь идею:

"Отлично! Сохраняю твою идею... 💾"

СИСТЕМНЫЕ ДЕЙСТВИЯ (выполняются автоматически через API Tool):
1. Отправить данные в Supabase через webhook/API:
   - user_telegram_id: {user_telegram_id}
   - draft_text: {draft_text}
   - created_at: текущая timestamp
   
2. Получить от Supabase в ответе:
   - request_id (ID записи в таблице requests)
   - feature_id (ID для голосования в канале)
   
3. Сохранить в переменные:
   - {request_id} = полученный request_id
   - {feature_id} = полученный feature_id
   
После успешного сохранения → путь "Сохранено в БД"
```

### API Tool Configuration:
**КАК НАСТРОИТЬ В VOICEFLOW:**
1. В этом Agent Step добавить API Tool
2. Name: `save_to_supabase`
3. Method: `POST`
4. URL: `{{env.SUPABASE_URL}}/rest/v1/requests`
5. Headers:
   - `apikey`: `{{env.SUPABASE_SERVICE_ROLE_KEY}}`
   - `Authorization`: `Bearer {{env.SUPABASE_SERVICE_ROLE_KEY}}`
   - `Content-Type`: `application/json`
   - `Prefer`: `return=representation`
6. Body (JSON):
```json
{
  "user_telegram_id": "{user_telegram_id}",
  "draft_text": "{draft_text}",
  "status": "pending_vote"
}
```
7. Response Mapping (сохранить ответ в переменные):
   - Variable `request_id` ← `response[0].id`
   - Variable `feature_id` ← `response[0].id` (feature_id = id в Supabase)

**ВАЖНО:** 
- `{{env.XXX}}` берется из Environment Variables (Project Settings)
- `{variable}` берется из обычных Variables
- После успешного вызова API переменные `request_id` и `feature_id` автоматически заполнятся

### Exits (Path Tools):
- ✅ **"Сохранено в БД"** → Агент 6
  - Description: "Данные сохранены в Supabase, получены request_id и feature_id"

**Модель**: Claude-4-Sonnet  
**Temperature**: 0.3  
**Max Tokens**: 5000

---

## 💳 Агент 6: Предложение клинического приоритета
**ID**: `agent_payment_offer`

### System Prompt:
```
Предложи пользователю купить 10 голосов для идеи:

"Идея зафиксирована! 📝

Теперь она попадёт на голосование в наш Telegram канал, где медики оценивают идеи и выбирают приоритеты для разработки.

**Хочешь ускорить процесс?**

За 1 ⭐ (Telegram Stars) ты можешь отметить идею как **клинически приоритетную** — это даст ей **+10 голосов** в канале сразу.

Это не гарантирует релиз, но резко повышает шансы на быструю реализацию, если идея наберёт поддержку.

Как поступим?"

ВАЖНО:
- Переменные {request_id} и {feature_id} уже сохранены на предыдущем шаге
- При выборе "pay" они будут переданы в payload для оплаты
```

### Button Tool (Inline Keyboard):
```json
{
  "buttons": [
    {
      "name": "pay",
      "label": "⭐ Оплатить приоритет (+10 голосов)"
    },
    {
      "name": "skip",
      "label": "👍 Нет, просто зафиксировать"
    }
  ]
}
```

### Exits (Path Tools):
- 💰 **"pay"** → Агент 7 (финальная благодарность)
  - Description: "Пользователь выбрал оплатить приоритет"
  - Action: 
    - Set variable `payment_status = "pending"`
    - Бот автоматически вызовет sendInvoice с payload: `{"kind":"clinical_priority","request_id":{request_id},"feature_id":{feature_id},"user_id":"{user_telegram_id}"}`
  
- ⏭️ **"skip"** → Агент 7 (финальная благодарность)
  - Description: "Пользователь пропустил оплату"
  - Action: Set variable `payment_status = "skipped"`

**Модель**: Claude-4-Sonnet  
**Temperature**: 0.3  
**Max Tokens**: 5000

**КРИТИЧЕСКИ ВАЖНО ДЛЯ ИНТЕГРАЦИИ:**
При нажатии кнопки "pay" бот (index.js) должен:
1. Прочитать переменные {request_id} и {feature_id} из Voiceflow state
2. Создать payload для invoice с этими данными
3. После успешной оплаты:
   - Обновить Supabase: `UPDATE requests SET payment_status='paid', votes=votes+10 WHERE id={request_id}`
   - Отправить сообщение в Telegram канал с feature_id
   - Отправить event в Voiceflow: `clinical_priority_paid`

---

## 🎉 Агент 7: Финальная благодарность
**ID**: `agent_final_thanks` и ОБЯЗАТЕЛЬНО укажи канал:

ЕСЛИ {payment_status} == "paid":
"Спасибо за поддержку! 🙏⭐

Твоя идея получила клинический приоритет и **+10 голосов** сразу!

📊 Она будет опубликована в канале для голосования:
{channel_link}

Следи за каналом — там увидишь свою идею и сможешь отслеживать прогресс. Ты будешь в числе первых, кто узнает о начале разработки!"

ЕСЛИ {payment_status} == "skipped":
"Спасибо за детальный рассказ! 🙏

Твоя идея зафиксирована (ID: #{feature_id}) и попадёт на голосование.

📊 Канал для голосования:
{channel_link}

Там появится твоя идея, и медики смогут проголосовать. Если наберёт поддержку — начнём разработку!"

В ЛЮБОМ СЛУЧАЕ добавить:
"
💡 Если появятся ещё рабочие ситуации, где что-то можно упростить — возвращайся! Каждая история помогает создавать действительно полезные инструменты."

КРИТИЧЕСКИ ВАЖНО:
- Переменная {channel_link} должна быть установлена в Агенте 1
- Переменная {feature_id} приходит из Supabase в Агенте 5
- ВСЕГДА показывай ссылку на канал в обоих случаях (paid и skipped)
Канал для голосования: [ссылка]"

В ЛЮБОМ СЛУЧАЕ добавить:
"Если появятся ещё рабочие ситуации, где что-то можно упростить — возвращайся! Каждая история помогает создавать полезные инструменты."
```

### Exits (Path Tools):
- Нет выходов (финальный агент)

**Модель**: Claude-4-Sonnet  
**Temperature**: 0.3  
**Max Tokens**: 5000

---

## 🚫 Агент 8: Мягкое завершение (для не-медиков)
**ID**: `agent_soft_exit`

### System Prompt:
```
Мягко завершить диалог:

"Спасибо за интерес! 🙂

Сейчас я фокусируюсь именно на медицинской сфере — помогаю врачам, медсёстрам, администраторам клиник формулировать идеи цифровых инструментов.

Если в будущем начнёшь работать в здравоохранении или знаешь кого-то из медиков — возвращайся!

Удачи! 👋"
```

### Exits (Path Tools):
- Нет выходов (финальный агент)

**Модель**: Claude-4-Sonnet  
**Temperature**: 0.3  
**Max Tokens**: 5000

---

## 🔗 Интеграция с Backend (index.js)

### Изменения в обработчике кнопки "pay":

```javascript
// В callback_query handler для кнопки "pay"
bot.on('callback_query', async (ctx) => {
    const data = ctx.callbackQuery.data;
    const userId = String(ctx.from.id);
    
    if (data === 'choice_X' && selectedButton.name === 'pay') {
        // 1. Получить переменные из Voiceflow state
        const stateUrl = `https://general-runtime.voiceflow.com/state/${VF_VERSION_ID}/user/${userId}/variables`;
        const stateResponse = await axios.get(stateUrl, {
            headers: { 'Authorization': VF_API_KEY }
        });
        
        const requestId = stateResponse.data.request_id;
        const featureId = stateResponse.data.feature_id;
        
        // 2. Создать payload для invoice
        const payload = {
            kind: 'clinical_priority',
            request_id: requestId,
            feature_id: featureId,
            user_id: userId,
            ts: Math.floor(Date.now() / 1000)
        };
        
        // 3. Отправить invoice
        await ctx.sendInvoice({
            title: '🧬 Клинический приоритет',
            description: '+10 голосов для твоей идеи в канале голосования',
            payload: JSON.stringify(payload),
            provider_token: '',
            currency: 'XTR',
            prices: [{ label: 'Клинический приоритет', amount: 1 }]
        });
    }
});

// В pre_checkout_query handler
bot.on('pre_checkout_query', async (ctx) => {
    await ctx.answerPreCheckoutQuery(true);
});

// В successful_payment handler
bot.on('successful_payment', async (ctx) => {
    const payload = JSON.parse(ctx.message.successful_payment.invoice_payload);
    const { request_id, feature_id, user_id } = payload;
    
    // 1. Обновить Supabase
    await supabase
        .from('requests')
        .update({ 
            payment_status: 'paid',
            votes: supabase.raw('votes + 10'),
            paid_at: new Date().toISOString()
        })
        .eq('id', request_id);
    
    // 2. Опубликовать в Telegram канал
    await bot.telegram.sendMessage(TELEGRAM_CHANNEL_ID, 
        `🆕 Новая идея с приоритетом!\n\nFeature #${feature_id}\n\n[текст из draft_text]\n\n👍 Уже 10 голосов!`
    );
    
    // 3. Отправить event в Voiceflow
    await voiceflowEvent(user_id, 'clinical_priority_paid', {
        request_id,
        feature_id,
        votes: 10
    });
    
    // 4. Обновить переменную payment_status в Voiceflow
    await axios.patch(
        `https://general-runtime.voiceflow.com/state/${VF_VERSION_ID}/user/${user_id}/variables`,
        { payment_status: 'paid' },
        { headers: { 'Authorization': VF_API_KEY } }
    );
});
```

---

## 📊 Структура базы данных Supabase

### Таблица: `requests`

```sql
CREATE TABLE requests (
    id SERIAL PRIMARY KEY,
    user_telegram_id VARCHAR(50) NOT NULL,
    draft_text TEXT NOT NULL,
    status VARCHAR(20) DEFAULT 'pending_vote',
    payment_status VARCHAR(20) DEFAULT 'unpaid',
    votes INTEGER DEFAULT 0,
    feature_id INTEGER GENERATED ALWAYS AS (id) STORED,
    created_at TIMESTAMP DEFAULT NOW(),
    paid_at TIMESTAMP,
    published_to_channel BOOLEAN DEFAULT FALSE
);

-- Индексы
CREATE INDEX idx_requests_user_telegram_id ON requests(user_telegram_id);
CREATE INDEX idx_requests_feature_id ON requests(feature_id);
CREATE INDEX idx_requests_status ON requests(status);
```

### API endpoint для создания request:

```javascript
// POST /rest/v1/requests
// Headers: 
//   apikey: SUPABASE_SERVICE_ROLE_KEY
//   Authorization: Bearer SUPABASE_SERVICE_ROLE_KEY
//   Prefer: return=representation

// Body:
{
    "user_telegram_id": "123456789",
    "draft_text": "Текст черновика продукта"
}

// Response:
{
    "id": 42,
    "user_telegram_id": "123456789",
    "draft_text": "Текст черновика продукта",
    "feature_id": 42,
    "votes": 0,
    "status": "pending_vote",
    "payment_status": "unpaid",
    "created_at": "2026-01-14T12:00:00Z"
}
```

---

## 🔄 Flow с учётом интеграций

```
START
  ↓
[1] Приветствие + квалификация
  │   Set: user_telegram_id = {user_id}
  ├─ Не медик → [8] Мягкое завершение → END
  └─ Медик ↓
  
[2] Сбор проблемы (только текст, БЕЗ файлов)
  ↓
[3] Углубление (5 вопросов в одном сообщении)
  ↓
[4] Формирование черновика
  │   Set: draft_text = [черновик]
  ↓
[5] Сохранение в Supabase
  │   API Call: POST /rest/v1/requests
  │   Get: request_id, feature_id
  │   Set: request_id, feature_id
  ↓
[6] 💰 Предложение клинического приоритета
  │
  ├─ [⭐ Оплатить] 
  │   │   Set: payment_status = "pending"
  │   │   → index.js: sendInvoice(request_id, feature_id)
  │   │   → User pays
  │   │   → index.js: UPDATE votes +10, publish to channel
  │   │   → index.js: voiceflowEvent('clinical_priority_paid')
  │   │   Set: payment_status = "paid"
  │   ↓
  └─ [👍 Пропустить] 
      │   Set: payment_status = "skipped"
      ↓
  

#### Шаг 1: Environment Variables (Project Settings)
- [ ] Открыть: Project Settings (⚙️ в правом верхнем углу) → Environment Variables
- [ ] Создать переменную `SUPABASE_URL` = `https://rohplqelrlwszotkmnir.supabase.co`
- [ ] Создать переменную `SUPABASE_SERVICE_ROLE_KEY` = ключ из Supabase
- [ ] (Опционально) `TELEGRAM_CHANNEL_LINK` если хочешь менять через env

#### Шаг 2: Variables (обычные переменные проекта)
- [ ] Открыть: Variables (левая панель, иконка `{x}`)
- [ ] Создать `request_id` (type: Number)
- [ ] Создать `feature_id` (type: Number)
- [ ] Создать `user_telegram_id` (type: Text)
- [ ] Создать `draft_text` (type: Text)
- [ ] Создать `payment_status` (type: Text)
- [ ] Создать `channel_link` (type: Text, default: `https://t.me/твой_канал`)

#### Шаг 3: Настройка агентов
- [ ] Все кнопки настроить как Inline Keyboard (Button Tool)
- [ ] В Агенте 5 создать API Tool для Supabase (см. конфигурацию выше)
- [ ] В Агенте 1 добавить Action: Set `channel_link` = ссылка на канал
- [ ] Убрать все упоминания загрузки файлов из промпт
END
```

---

## ✅ Чек-лист интеграции

### В Voiceflow:
- [ ] Создать 7 переменных (Variables): request_id, feature_id, user_telegram_id, draft_text, payment_status
- [ ] Все кнопки настроить как Inline Keyboard
- [ ] В Агенте 5 настроить API Tool для Supabase
- [ ] Убрать все упоминания загрузки файлов

### В index.js:
- [ ] Обработчик кнопки "pay": читать request_id и feature_id из Voiceflow state
- [ ] successful_payment: обновлять Supabase (+10 votes)
- [ ] successful_payment: публиковать в Telegram канал
- [ ] successful_payment: отправлять event в Voiceflow
- [ ] successful_payment: обновлять payment_status в Voiceflow

### В Supabase:
- [ ] Создать таблицу requests с полями: id, user_telegram_id, draft_text, votes, payment_status, feature_id
- [ ] Настроить RLS (Row Level Security) если нужно
- [ ] Проверить что API endpoint возвращает feature_id

### В .env:
- [ ] SUPABASE_URL
- [ ] SUPABASE_SERVICE_ROLE_KEY
- [ ] TELEGRAM_CHANNEL_ID

---

## 🚀 Следующие шаги

1. **Обсудить промпты** → внести правки если нужно
2. **Создать JSON для импорта** → готовый project-full-optimized.json
3. **Настроить Supabase** → создать таблицу requests
4. **Обновить index.js** → добавить обработчики оплаты
5. **Импортировать в Voiceflow** → новый проект
6. **Протестировать флоу** → /start → описание → оплата → проверить Supabase

Готов создать финальный JSON файл? 🚀
