# 🚀 Быстрая настройка Supabase

## ✅ Безопасность

Все SQL файлы используют `CREATE TABLE IF NOT EXISTS` - они:
- ✅ НЕ удаляют существующие данные
- ✅ НЕ пересоздают таблицы
- ✅ Только создают то, чего ещё нет
- ✅ Можно выполнять повторно без вреда

---

## 📝 Что выполнить в Supabase SQL Editor

Открой Supabase → SQL Editor и выполни по очереди:

### 1. votes.sql ⭐ ОБЯЗАТЕЛЬНО
Система голосования (1 голос на юзера)
```bash
Скопируй всё из votes.sql → Run
```

### 2. payments.sql ⭐ ОБЯЗАТЕЛЬНО
Платежи Telegram Stars
```bash
Скопируй всё из payments.sql → Run
```

### 3. conversations.sql ⭐ ОБЯЗАТЕЛЬНО
Логи AI диалогов для аналитики
```bash
Скопируй всё из conversations.sql → Run
```

### 4. migration-full-description.sql ⭐ УЖЕ ВЫПОЛНЕНО
Добавляет колонку full_description в requests
```bash
✅ Ты уже выполнил - пропусти
```

### 5. system-messages.sql 📌 Опционально
Для закрепленного топ-поста (если будешь использовать update-top-ideas.js)
```bash
Скопируй всё из system-messages.sql → Run
```

---

## ⚡ Быстрый способ (всё сразу)

Можно выполнить все файлы одной командой в SQL Editor:

```sql
-- votes.sql
[вставь содержимое votes.sql]

-- payments.sql
[вставь содержимое payments.sql]

-- conversations.sql
[вставь содержимое conversations.sql]

-- system-messages.sql (опционально)
[вставь содержимое system-messages.sql]
```

Нажми Run один раз.

---

## 🔍 Проверка

После выполнения проверь что таблицы созданы:

Supabase → Table Editor → должны быть:
- ✅ requests (создаётся автоматически при первой публикации)
- ✅ votes
- ✅ payments
- ✅ conversations
- ✅ system_messages (если выполнил)

---

## ❓ Что НЕ НАДО делать

❌ НЕ выполняй cleanup-production.js (он удалит все данные)
❌ НЕ удаляй существующие таблицы вручную
❌ НЕ выполняй DROP TABLE команды

---

## 📊 Текущее состояние

У тебя уже есть:
- ✅ migration-full-description.sql - выполнено
- ⏳ votes.sql - нужно выполнить
- ⏳ payments.sql - нужно выполнить  
- ⏳ conversations.sql - нужно выполнить
- ⏳ system-messages.sql - опционально

**Итого:** Выполни 3 обязательных файла (votes, payments, conversations)
