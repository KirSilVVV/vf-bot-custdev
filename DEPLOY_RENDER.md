# 🚀 Деплой на Render - Инструкция

## ✅ Что нужно сделать на Render

### 1. Зайти в Dashboard Render
Открыть: https://dashboard.render.com/

### 2. Найти свой сервис
Найти сервис **vf-telegram-bot** (или создать новый)

### 3. Обновить Environment Variables

Перейти в **Environment** и добавить/обновить эти переменные:

```
TELEGRAM_BOT_TOKEN = <твой токен от BotFather>
TELEGRAM_CHANNEL_ID = -1003597143093
SUPABASE_URL = <твой Supabase URL>
SUPABASE_SERVICE_ROLE_KEY = <твой Supabase service role key>
BOTPRESS_API_KEY = bp_bak_mOcOmZ06_bCWCYxOPxlqh2O8drVnD1rSzh8A
NODE_ENV = production
```

### 4. Изменить Start Command (если нужно)

В настройках сервиса → **Settings** → **Start Command**:
```
node index-botpress.js
```

### 5. Задеплоить

**Вариант A: Автоматический деплой через Git**
1. Сделать commit:
   ```powershell
   git add .
   git commit -m "Add Botpress bot with voting buttons and payments"
   git push
   ```
2. Render автоматически задеплоит

**Вариант B: Manual Deploy**
1. В Dashboard Render → **Manual Deploy** → **Deploy latest commit**

### 6. Проверить логи

После деплоя проверить **Logs** на Render:
- Должно быть: `✅ Telegram bot started (polling mode)` или webhook mode
- Должно быть: `✅ Express server на порту 3000`

## 🧪 Тестирование после деплоя

1. **Открыть бота в Telegram**
   - Найти своего бота (имя из BotFather)
   - Нажать `/start`
   - Должно прийти приветственное сообщение

2. **Отправить идею**
   - Написать любую идею, например: "Добавить темную тему"
   - Бот должен ответить
   - Проверить канал - там должна появиться карточка с кнопками

3. **Проголосовать**
   - Перейти в канал
   - Нажать 👍 или 👎
   - Счетчик должен обновиться

4. **Тест оплаты**
   - Нажать "⭐ Клинический приоритет"
   - Должен открыться инвойс на 300 Stars
   - После оплаты - +10 голосов

## ❌ Проблемы?

### Бот не отвечает на /start
- Проверить TELEGRAM_BOT_TOKEN в Render
- Проверить логи: есть ли ошибки подключения

### Кнопки не работают
- Проверить SUPABASE_URL и SUPABASE_SERVICE_ROLE_KEY
- Проверить что таблица `requests` существует в Supabase

### Нет постов в канале
- Проверить TELEGRAM_CHANNEL_ID (должен начинаться с `-100`)
- Проверить что бот добавлен как администратор канала

## 📋 Checklist перед деплоем

- [ ] Все переменные окружения добавлены в Render
- [ ] Бот добавлен как админ в Telegram канал
- [ ] Таблицы `requests` и `payments` созданы в Supabase
- [ ] Start command = `node index-botpress.js`
- [ ] Код закоммичен в Git
