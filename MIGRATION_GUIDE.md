# Инструкция по применению миграции

## Что изменилось

Теперь бот проводит детальный опрос (5 вопросов) для формирования продуктовой фичи:

1. **Проблема** - Какую проблему решаем?
2. **Текущее решение** - Как решают сейчас? Что не устраивает?
3. **Желаемое решение** - Как должно работать?
4. **Критерии реализуемости** - Минимум функций для MVP
5. **Пользовательский сценарий** - Шаги использования

## Результат опроса

AI генерирует два варианта:

- **📋 ПОЛНОЕ ОПИСАНИЕ** - сохраняется в базе в поле `full_description` для дальнейшей реализации
- **📢 КРАТКИЙ ДРАФТ** (до 300 символов) - публикуется в канале в поле `request_text`

## Применение миграции в Supabase

1. Открой Supabase Dashboard: https://supabase.com/dashboard/project/YOUR_PROJECT
2. Перейди в SQL Editor
3. Скопируй и выполни содержимое файла `migration-full-description.sql`
4. Нажми "Run"

## Что добавляется в БД

```sql
ALTER TABLE requests
ADD COLUMN IF NOT EXISTS full_description TEXT;
```

- Колонка `full_description` - полное описание фичи
- Колонка `request_text` - краткая версия для канала (до 300 символов)

## Проверка

После применения миграции новые идеи будут сохраняться с двумя версиями:
- В канале: короткая версия (легко читается)
- В базе: полная версия (для реализации ИИ)

Старые записи автоматически получат `full_description = request_text`.
