# 🚀 Quick Setup Guide

## 📋 SQL Scripts to Run in Supabase

### 1. **conversations.sql** - AI Chat Analytics
Создает таблицу для хранения всех диалогов с OpenAI.

**Что делает:**
- Сохраняет каждое сообщение пользователя и ответ AI
- Отслеживает номер сообщения в диалоге
- Помечает готовность идеи к публикации
- Фиксирует факт публикации

**Как запустить:**
1. Открой **Supabase Dashboard** → SQL Editor
2. Скопируй весь код из [conversations.sql](conversations.sql)
3. Вставь в SQL Editor
4. Нажми **Run** (или Ctrl+Enter)
5. ✅ Должно показать: "Success. No rows returned"

---

### 2. **votes.sql** - Voting System (1 vote per user)
Создает таблицу для отслеживания голосов с ограничением: **1 юзер = 1 голос за идею**.

**Что делает:**
- Записывает кто и за что голосовал (👍 или 👎)
- Предотвращает повторные голоса (UNIQUE constraint)
- Разрешает изменить голос (с "за" на "против")

**Как запустить:**
1. Supabase Dashboard → SQL Editor
2. Скопируй весь код из [votes.sql](votes.sql)
3. Вставь и нажми **Run**
4. ✅ "Success. No rows returned"

---

## 🎯 Как редактировать системный промпт OpenAI

### Где находится промпт?
Файл: [ai-helper.js](ai-helper.js) (строки 4-24)

### Как изменить?

1. **Открой файл** [ai-helper.js](ai-helper.js)

2. **Найди переменную** `SYSTEM_PROMPT` (начало файла):

```javascript
const SYSTEM_PROMPT = `Ты - помощник по Customer Development...`
```

3. **Измени текст промпта** как нужно. Например:

```javascript
const SYSTEM_PROMPT = `Ты - веселый помощник по сбору идей!

Задавай вопросы кратко и весело:
1. Спроси про проблему 🤔
2. Спроси про решение 💡
3. Предложи опубликовать 🎉

Используй эмодзи и будь позитивным!`
```

4. **Сохрани файл** (Ctrl+S)

5. **Перезапусти бота:**
   - Останови текущий процесс (Ctrl+C в терминале)
   - Запусти снова: `node index-botpress.js`

### Примеры изменений промпта:

#### Вариант 1: Быстрый сбор (1 вопрос)
```javascript
const SYSTEM_PROMPT = `Помоги пользователю описать идею за 1 вопрос.

Спроси: "Опиши идею в одном предложении - что хочешь улучшить?"

После ответа скажи: "Отлично, готово к публикации! 🎉"`
```

#### Вариант 2: Подробный сбор (5 вопросов)
```javascript
const SYSTEM_PROMPT = `Проведи детальное интервью:
1. Какая проблема?
2. Как часто возникает?
3. Кто еще сталкивается?
4. Какое решение видишь?
5. Что даст это решение?

После 5 ответов предложи публикацию.`
```

#### Вариант 3: Для B2B продукта
```javascript
const SYSTEM_PROMPT = `Помоги сформулировать enterprise-запрос:
1. В каком отделе проблема? (IT, Sales, HR...)
2. Сколько людей затрагивает?
3. Какое решение нужно?

После 3 вопросов готовь к публикации.`
```

### Настройки AI модели

В том же файле можно изменить параметры модели (строка 34):

```javascript
const response = await openai.chat.completions.create({
    model: 'gpt-4o-mini',        // Модель (gpt-4o, gpt-4o-mini)
    messages: messages,
    temperature: 0.7,             // Креативность (0-2, больше = креативнее)
    max_tokens: 200,              // Макс. длина ответа
});
```

**Temperature:**
- `0.3` - строгие, предсказуемые ответы
- `0.7` - баланс (по умолчанию)
- `1.2` - креативные, разнообразные ответы

**Max tokens:**
- `100` - короткие ответы (1-2 предложения)
- `200` - средние (по умолчанию)
- `500` - длинные подробные ответы

### Как изменить количество вопросов до публикации?

В [ai-helper.js](ai-helper.js) (строка 46):

```javascript
export function shouldOfferPublish(messageCount) {
    return messageCount >= 3;  // ← Измени на 2, 4, 5 и т.д.
}
```

---

## ✅ Проверка что всё работает

### 1. Проверить таблицы в Supabase:
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
AND table_name IN ('conversations', 'votes', 'requests', 'payments');
```
Должно вернуть 4 строки.

### 2. Протестировать бота:
1. Напиши боту идею
2. AI должен задать вопросы (проверь консоль - должно быть "✅ OpenAI response")
3. После 3 сообщений показываются кнопки публикации
4. Опубликуй в канал
5. Попробуй проголосовать 2 раза - должно показать "✋ Ты уже проголосовал"

### 3. Проверить логи диалогов:
```sql
SELECT * FROM conversations ORDER BY created_at DESC LIMIT 10;
```

### 4. Проверить голоса:
```sql
SELECT * FROM votes ORDER BY created_at DESC LIMIT 10;
```

---

## 🔧 Troubleshooting

### AI не отвечает:
- Проверь `OPENAI_API_KEY` в [.env](.env)
- Посмотри консоль - должен быть "✅ OpenAI response" или ошибка API

### Голосование не работает:
- Убедись что таблица `votes` создана в Supabase
- Проверь логи: должно быть "✅ New vote recorded"

### Диалоги не сохраняются:
- Проверь таблицу `conversations` в Supabase
- Посмотри консоль: должно быть "📊 Conversation logged"

---

## 🎨 Полезные ссылки

- **Supabase Dashboard**: https://app.supabase.com/project/rohplqelrlwszotkmnir
- **OpenAI Playground**: https://platform.openai.com/playground (для тестирования промптов)
- **Аналитика**: См. [ANALYTICS_GUIDE.md](ANALYTICS_GUIDE.md)
