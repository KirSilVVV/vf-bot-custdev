# 🚀 Пошаговая инструкция создания Customer Development Bot в Voiceflow

## Подготовка (5 минут)

### Шаг 1: Создать новый проект
1. Открыть Voiceflow.com → Dashboard
2. **Create New Agent**
3. Name: `Customer Development Bot v2`
4. Platform: **Chat** (не Voice!)
5. Click **Create**

---

## Настройка переменных (3 минуты)

### Шаг 2: Environment Variables
1. Открыть **Project Settings** (⚙️ в правом верхнем углу)
2. Перейти в **Environment Variables**
3. Добавить переменные:

```
Название: SUPABASE_URL
Значение: https://rohplqelrlwszotkmnir.supabase.co

Название: SUPABASE_SERVICE_ROLE_KEY
Значение: [твой ключ из Supabase → Settings → API → service_role]
```

4. **Save**

### Шаг 3: Variables (обычные переменные)
1. На левой панели кликнуть **Variables** (иконка `{x}`)
2. **+ Create Variable** для каждой:

```
Название: request_id
Тип: Number
Описание: ID записи в Supabase

Название: feature_id  
Тип: Number
Описание: ID фичи для голосования

Название: user_telegram_id
Тип: Text
Описание: Telegram ID пользователя

Название: draft_text
Тип: Text
Описание: Текст черновика продукта

Название: payment_status
Тип: Text
Описание: Статус оплаты

Название: channel_link
Тип: Text
Default Value: https://t.me/твой_канал_голосования
```

---

## Создание агентов (30 минут)

### Шаг 4: Создать 8 AI Agents

#### 🎯 Агент 1: Приветствие и квалификация

1. На Canvas нажать **+** → **Agent**
2. Name: `Приветствие и квалификация`
3. **Instructions (System Prompt):**

```
Поздоровайся с пользователем и объясни суть:

"Привет! 👋 Я помогаю медицинским специалистам формулировать идеи цифровых инструментов на основе реальных рабочих ситуаций.

Речь не о готовых решениях, а о выявлении моментов в работе, где что-то можно упростить или автоматизировать. Не нужно заранее придумывать продукт — просто расскажешь о конкретной ситуации, а я помогу сформулировать черновик.

По итогам обсуждения:
• Идея попадёт на голосование в Telegram канал
• Если наберёт поддержку — может быть реализована
• Ты получишь процент от использования инструмента

Ты работаешь в медицине или здравоохранении?"
```

4. **Model Settings:**
   - Model: Claude 4 Sonnet
   - Temperature: 0.3
   - Max Tokens: 5000

5. **Button Tool:**
   - Enable **Buttons**
   - Add button: `✅ Да, работаю в медицине` → name: `yes_medical`
   - Add button: `❌ Нет` → name: `no_medical`

6. **Paths (Exits):**
   - Add Path: `yes_medical`
     - Name: "Работает в медицине"
     - Description: "Пользователь подтвердил"
     - **Actions:**
       - Set Variable `user_telegram_id` = `{user_id}`
       - Set Variable `channel_link` = `https://t.me/твой_канал`
   
   - Add Path: `no_medical`
     - Name: "Не работает в медицине"
     - Description: "Пользователь отказался"

---

#### 📝 Агент 2: Сбор проблемы

1. Добавить новый **Agent**: `Сбор проблемы`
2. **Instructions:**

```
Попроси пользователя описать конкретную рабочую ситуацию ТЕКСТОМ:

"Отлично! Теперь расскажи о конкретной ситуации на работе, где возникла мысль: 'Здесь должно быть проще' или 'Это можно автоматизировать'.

Важно: не абстрактная идея инструмента, а именно момент из практики, где что-то вызвало сложность или раздражение.

Опиши ситуацию текстом — максимально конкретно, с деталями."

ВАЖНО: 
- НЕ предлагай загрузку файлов
- НЕ упоминай скриншоты, фото, PDF
- Только текстовое описание

После получения описания сразу переходи к следующему шагу.
```

3. Model Settings: Claude 4 Sonnet, Temperature 0.3, Max Tokens 5000

4. **Path:**
   - Add Path: `description_received`
   - Name: "Получено описание"
   - Description: "Пользователь описал ситуацию"

---

#### 🔍 Агент 3: Углубление в контекст

1. Добавить **Agent**: `Углубление в контекст`
2. **Instructions:**

```
Проанализируй полученное описание и задай 4-5 уточняющих вопросов В ОДНОМ СООБЩЕНИИ:

"Понял ситуацию! Чтобы лучше разобраться, уточни несколько моментов:

1️⃣ На каком этапе работы это происходит?
   (до приёма / во время приёма / после / администрирование / принятие решения / другое)

2️⃣ Что именно ты пытаешься сделать или понять в этот момент?

3️⃣ Как часто сталкиваешься с этой ситуацией?
   (каждый день / несколько раз в неделю / раз в неделю / раз в месяц / реже)

4️⃣ Сколько времени/нервов это отнимает?
   (назови примерно: минуты, часы, дни?)

5️⃣ Как сейчас справляешься с этой ситуацией без цифрового инструмента?"

После получения ответов переходи к следующему шагу через путь "Контекст собран".
```

3. **Path:**
   - Add Path: `context_collected`
   - Name: "Контекст собран"

---

#### 📋 Агент 4: Формирование черновика

1. Добавить **Agent**: `Формирование черновика`
2. **Instructions:**

```
На основе всего предыдущего диалога сформулируй черновое описание продукта И СОХРАНИ ЕГО В ПЕРЕМЕННУЮ:

"Вот что я понял из твоего рассказа:

📍 **Рабочая ситуация:**
[В каком моменте работы возникает сложность]

❌ **Что не работает сейчас:**
[Что именно вызывает затруднение]

🎯 **Задача специалиста:**
[Что специалист пытается сделать или понять]

💡 **Возможный цифровой инструмент:**
[Какой инструмент мог бы помогать]

⚖️ **Границы ответственности:**
[Что остаётся за специалистом, где инструмент только помогает, а не решает]

---

Насколько точно я понял? Можешь уточнить или согласиться продолжить."

КРИТИЧЕСКИ ВАЖНО:
1. Сохрани ВЕСЬ черновик в переменную {draft_text}
2. Этот текст будет отправлен в Supabase и Telegram канал
3. НЕ использовать слова: "фича", "продукт", "стартап", "платформа", "ИИ"
4. НЕ давать медицинских рекомендаций

После согласования пользователя → путь "Черновик согласован"
```

3. **Button Tool:**
   - Add button: `✅ Всё верно, продолжаем` → name: `draft_approved`
   - Add button: `✏️ Хочу уточнить` → name: `draft_edit`

4. **Paths:**
   - Add Path: `draft_approved`
     - Name: "Черновик согласован"
     - **Action:** Set Variable `draft_text` = `{last_response}` (весь текст черновика)
   
   - Add Path: `draft_edit`
     - Name: "Уточнить черновик"
     - Connect back to this Agent

---

#### 💾 Агент 5: Сохранение в Supabase

1. Добавить **Agent**: `Сохранение в Supabase`
2. **Instructions:**

```
Скажи пользователю что сохраняешь идею:

"Отлично! Сохраняю твою идею... 💾"

СИСТЕМНЫЕ ДЕЙСТВИЯ выполняются автоматически через API Tool.
После успешного сохранения → путь "Сохранено в БД"
```

3. **API Tool Configuration:**
   - Click **+ Add Tool** → **API**
   - Name: `save_to_supabase`
   - Method: **POST**
   - URL: `{{env.SUPABASE_URL}}/rest/v1/requests`
   
   - **Headers:**
     ```
     apikey: {{env.SUPABASE_SERVICE_ROLE_KEY}}
     Authorization: Bearer {{env.SUPABASE_SERVICE_ROLE_KEY}}
     Content-Type: application/json
     Prefer: return=representation
     ```
   
   - **Body (JSON):**
     ```json
     {
       "user_telegram_id": "{user_telegram_id}",
       "draft_text": "{draft_text}",
       "status": "pending_vote"
     }
     ```
   
   - **Response Mapping:**
     - Map `response[0].id` → Variable `request_id`
     - Map `response[0].id` → Variable `feature_id`

4. **Path:**
   - Add Path: `saved_to_db`
   - Name: "Сохранено в БД"

---

#### 💳 Агент 6: Предложение клинического приоритета

1. Добавить **Agent**: `Предложение клинического приоритета`
2. **Instructions:**

```
Предложи пользователю купить 10 голосов для идеи:

"Идея зафиксирована! 📝

Теперь она попадёт на голосование в наш Telegram канал, где медики оценивают идеи и выбирают приоритеты для разработки.

**Хочешь ускорить процесс?**

За 1 ⭐ (Telegram Stars) ты можешь отметить идею как **клинически приоритетную** — это даст ей **+10 голосов** в канале сразу.

Это не гарантирует релиз, но резко повышает шансы на быструю реализацию, если идея наберёт поддержку.

Как поступим?"

ВАЖНО:
- Переменные {request_id} и {feature_id} уже сохранены на предыдущем шаге
- При выборе "pay" они будут переданы в payload для оплаты
```

3. **Button Tool:**
   - Add button: `⭐ Оплатить приоритет (+10 голосов)` → name: `pay`
   - Add button: `👍 Нет, просто зафиксировать` → name: `skip`

4. **Paths:**
   - Add Path: `pay`
     - Name: "Оплата выбрана"
     - **Action:** Set Variable `payment_status` = `pending`
   
   - Add Path: `skip`
     - Name: "Оплата пропущена"
     - **Action:** Set Variable `payment_status` = `skipped`

---

#### 🎉 Агент 7: Финальная благодарность

1. Добавить **Agent**: `Финальная благодарность`
2. **Instructions:**

```
Поблагодари пользователя с учётом статуса оплаты и ОБЯЗАТЕЛЬНО укажи канал:

ЕСЛИ {payment_status} == "paid":
"Спасибо за поддержку! 🙏⭐

Твоя идея получила клинический приоритет и **+10 голосов** сразу!

📊 Она будет опубликована в канале для голосования:
{channel_link}

Следи за каналом — там увидишь свою идею и сможешь отслеживать прогресс. Ты будешь в числе первых, кто узнает о начале разработки!"

ЕСЛИ {payment_status} == "skipped":
"Спасибо за детальный рассказ! 🙏

Твоя идея зафиксирована (ID: #{feature_id}) и попадёт на голосование.

📊 Канал для голосования:
{channel_link}

Там появится твоя идея, и медики смогут проголосовать. Если наберёт поддержку — начнём разработку!"

В ЛЮБОМ СЛУЧАЕ добавить:
"
💡 Если появятся ещё рабочие ситуации, где что-то можно упростить — возвращайся! Каждая история помогает создавать действительно полезные инструменты."

КРИТИЧЕСКИ ВАЖНО:
- Переменная {channel_link} должна быть установлена в Агенте 1
- Переменная {feature_id} приходит из Supabase в Агенте 5
- ВСЕГДА показывай ссылку на канал в обоих случаях (paid и skipped)
```

3. **No Paths** (финальный агент)

---

#### 🚫 Агент 8: Мягкое завершение

1. Добавить **Agent**: `Мягкое завершение`
2. **Instructions:**

```
Мягко завершить диалог:

"Спасибо за интерес! 🙂

Сейчас я фокусируюсь именно на медицинской сфере — помогаю врачам, медсёстрам, администраторам клиник формулировать идеи цифровых инструментов.

Если в будущем начнёшь работать в здравоохранении или знаешь кого-то из медиков — возвращайся!

Удачи! 👋"
```

3. **No Paths** (финальный агент)

---

## Соединение блоков (10 минут)

### Шаг 5: Настроить Flow на Canvas

1. Найти **Start** блок на Canvas
2. Подключить **Start** → **Агент 1** (перетащить линию)

3. Подключить пути:
   ```
   Агент 1 (yes_medical) → Агент 2
   Агент 1 (no_medical) → Агент 8
   
   Агент 2 (description_received) → Агент 3
   
   Агент 3 (context_collected) → Агент 4
   
   Агент 4 (draft_approved) → Агент 5
   Агент 4 (draft_edit) → Агент 4 (loop back)
   
   Агент 5 (saved_to_db) → Агент 6
   
   Агент 6 (pay) → Агент 7
   Агент 6 (skip) → Агент 7
   
   Агент 7 → (no connections, end)
   Агент 8 → (no connections, end)
   ```

4. **Publish** проект (кнопка Publish в правом верхнем углу)

---

## Получение ключей (2 минуты)

### Шаг 6: Получить API ключи

1. Открыть **Project Settings** (⚙️)
2. Перейти в **API Keys**
3. **Create New Key**
4. Скопировать:
   - **API Key** (начинается с `VF.DM.xxx`)
   - **Version ID** (под названием проекта)

5. Обновить `.env`:
   ```
   VOICEFLOW_API_KEY=VF.DM.новый_ключ
   VOICEFLOW_VERSION_ID=новый_version_id
   ```

---

## Настройка Supabase (5 минут)

### Шаг 7: Создать таблицу requests

1. Открыть Supabase Dashboard → SQL Editor
2. Выполнить:

```sql
CREATE TABLE requests (
    id SERIAL PRIMARY KEY,
    user_telegram_id VARCHAR(50) NOT NULL,
    draft_text TEXT NOT NULL,
    status VARCHAR(20) DEFAULT 'pending_vote',
    payment_status VARCHAR(20) DEFAULT 'unpaid',
    votes INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW(),
    paid_at TIMESTAMP,
    published_to_channel BOOLEAN DEFAULT FALSE
);

CREATE INDEX idx_requests_user_telegram_id ON requests(user_telegram_id);
CREATE INDEX idx_requests_status ON requests(status);
```

3. Проверить: Settings → API → получить `service_role` key

---

## Обновление index.js (15 минут)

### Шаг 8: Добавить обработчик оплаты

В файл `index.js` добавить/обновить:

```javascript
// В callback_query handler
bot.on('callback_query', async (ctx) => {
    const data = ctx.callbackQuery.data;
    const userId = String(ctx.from.id);
    
    // ... existing button handling ...
    
    if (selectedButton && selectedButton.name === 'pay') {
        try {
            // 1. Получить переменные из Voiceflow
            const stateUrl = `https://general-runtime.voiceflow.com/state/${VF_VERSION_ID}/user/${userId}/variables`;
            const stateResponse = await axios.get(stateUrl, {
                headers: { 'Authorization': VF_API_KEY }
            });
            
            const requestId = stateResponse.data.request_id;
            const featureId = stateResponse.data.feature_id;
            
            // 2. Создать invoice
            const payload = {
                kind: 'clinical_priority',
                request_id: requestId,
                feature_id: featureId,
                user_id: userId,
                ts: Math.floor(Date.now() / 1000)
            };
            
            await ctx.sendInvoice({
                title: '🧬 Клинический приоритет',
                description: '+10 голосов для твоей идеи в канале',
                payload: JSON.stringify(payload),
                provider_token: '',
                currency: 'XTR',
                prices: [{ label: 'Клинический приоритет', amount: 1 }]
            });
            
            console.log('✅ Invoice sent for request_id:', requestId);
        } catch (err) {
            console.error('❌ Failed to send invoice:', err.message);
        }
        return;
    }
});

// В successful_payment handler
bot.on('successful_payment', async (ctx) => {
    const payload = JSON.parse(ctx.message.successful_payment.invoice_payload);
    const { request_id, feature_id, user_id } = payload;
    
    console.log('💰 Payment received for request_id:', request_id);
    
    try {
        // 1. Обновить Supabase
        const { error } = await supabase
            .from('requests')
            .update({ 
                payment_status: 'paid',
                votes: supabase.raw('votes + 10'),
                paid_at: new Date().toISOString()
            })
            .eq('id', request_id);
        
        if (error) throw error;
        
        // 2. Получить draft_text из Supabase
        const { data: requestData } = await supabase
            .from('requests')
            .select('draft_text')
            .eq('id', request_id)
            .single();
        
        // 3. Опубликовать в канал
        await bot.telegram.sendMessage(
            TELEGRAM_CHANNEL_ID,
            `🆕 Новая идея с приоритетом!\n\nFeature #${feature_id}\n\n${requestData.draft_text}\n\n👍 Уже 10 голосов!`
        );
        
        // 4. Обновить payment_status в Voiceflow
        await axios.patch(
            `https://general-runtime.voiceflow.com/state/${VF_VERSION_ID}/user/${user_id}/variables`,
            { payment_status: 'paid' },
            { headers: { 'Authorization': VF_API_KEY } }
        );
        
        // 5. Отправить event в Voiceflow (опционально)
        await voiceflowEvent(user_id, 'clinical_priority_paid', {
            request_id,
            feature_id,
            votes: 10
        });
        
        console.log('✅ Payment processed successfully');
        
    } catch (err) {
        console.error('❌ Failed to process payment:', err.message);
    }
});
```

---

## Тестирование (10 минут)

### Шаг 9: Протестировать флоу

1. Запустить бота: `node index.js`
2. В Telegram отправить `/start`
3. Пройти весь флоу:
   - ✅ Подтвердить работу в медицине
   - 📝 Описать проблему
   - ✏️ Ответить на 5 вопросов
   - ✅ Согласовать черновик
   - 💾 Дождаться сохранения
   - ⭐ Выбрать оплату или пропустить
   - 🎉 Получить финальное сообщение

4. Проверить Supabase:
   ```sql
   SELECT * FROM requests ORDER BY created_at DESC LIMIT 1;
   ```

5. Проверить канал (если была оплата)

---

## ✅ Чек-лист

- [ ] Создан проект в Voiceflow
- [ ] Настроены Environment Variables (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
- [ ] Созданы 6 Variables (request_id, feature_id, user_telegram_id, draft_text, payment_status, channel_link)
- [ ] Созданы 8 агентов с промптами
- [ ] Настроены Button Tools (Inline Keyboard)
- [ ] Настроен API Tool в Агенте 5
- [ ] Соединены все блоки на Canvas
- [ ] Опубликован проект (Publish)
- [ ] Получены API ключи
- [ ] Создана таблица requests в Supabase
- [ ] Обновлен index.js (обработчик оплаты)
- [ ] Обновлен .env (VOICEFLOW_API_KEY, VOICEFLOW_VERSION_ID)
- [ ] Протестирован флоу от /start до конца

---

## 🎯 Результат

После выполнения всех шагов у вас будет:
- ✅ Оптимизированный бот (7 агентов вместо 13)
- ✅ Интеграция с Supabase
- ✅ Монетизация через Telegram Stars
- ✅ Публикация в Telegram канал
- ✅ Отслеживание feature_id и request_id

**Время на настройку:** ~1 час  
**Конверсия:** увеличится с 20-30% до 50-60%

Если возникнут вопросы — пиши! 🚀
